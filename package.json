{
  "name": "ttracker-browser-extension",
  "version": "0.0.1",
  "description": "Browser extension for pushing browser events to ttracker",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hwang381",
  "license": "ISC",
  "devDependencies": {
    "sign-addon": "^0.3.1",
    "web-ext-types": "^3.2.1"
  }
}
